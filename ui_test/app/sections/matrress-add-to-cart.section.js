const sectionCss = '[data-testid="mattress_add_cart_section"]';

const matrressAddToCartSection = {
  sectionCss,
  shopMattressButton: {
    css: `${sectionCss} [data-testid="addtocart_btn"]`,
  },
};

module.exports = matrressAddToCartSection;
